# @package _global_

# ViT-S/16 with gated self-attention in every encoder block.
# to execute this experiment run:
#   uv run src/train.py experiment=gated_attention

defaults:
  - override /module: vit_s_16_gated_attention
  - override /datamodule: default_q50

seed: 42

# Remember to define experiment_name and tags in all your experiments!
# First tag options: data, explore, baseline, ablate, hyperparam, final, <custom tag>
# Second tag options: train, pre-train, post-train, debug, <custom tag>
experiment_name: "gated_attention"
tags: ["explore", "train"]


datamodule:
  num_workers_train: 2